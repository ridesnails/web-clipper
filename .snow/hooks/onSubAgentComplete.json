{
  "onSubAgentComplete": [
    {
      "description": "Notify when sub-agent completes",
      "hooks": [
        {
          "type": "command",
          "command": "osascript -e 'display notification \"子代理任务已完成\" with title \"Snow CLI\"'",
          "enabled": true
        }
      ]
    }
  ]
}
